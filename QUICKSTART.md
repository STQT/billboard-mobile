# Быстрый старт Billboard Mobile

## ⚡ Минимальная установка (5 минут)

### Шаг 1: Подготовка окружения

```bash
# Убедитесь что установлены:
# - Python 3.11+
# - Flutter 3.0+
# - Docker (опционально)
```

### Шаг 2: Backend

```bash
# Перейти в директорию backend
cd billboard-mobile/backend

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows

# Установить зависимости
pip install -r requirements.txt

# Настроить .env
cp .env.example .env
# Редактировать .env при необходимости

# Запустить PostgreSQL и Redis через Docker
docker-compose up -d postgres redis

# Или использовать локальные установки
# PostgreSQL должен быть на localhost:5432
# Redis должен быть на localhost:6379

# Запустить сервер
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Проверка:** Откройте http://localhost:8000/docs - должна открыться Swagger документация

### Шаг 3: Тестирование Backend

В новом терминале:

```bash
cd billboard-mobile/backend
python test_api.py
```

Вы должны увидеть результаты всех тестов. ✅

### Шаг 4: Mobile приложение

```bash
# Перейти в директорию mobile
cd billboard-mobile/mobile

# Установить зависимости
flutter pub get

# Настроить URL backend
# Открыть lib/services/api_service.dart
# Изменить baseUrl:
#   - Для Android эмулятора: http://10.0.2.2:8000/api/v1
#   - Для iOS симулятора: http://localhost:8000/api/v1
#   - Для реального устройства: http://<YOUR_IP>:8000/api/v1

# Запустить приложение
flutter run

# Или для конкретной платформы:
flutter run -d android
flutter run -d ios
```

### Шаг 4.5: Admin Panel (Веб-интерфейс) - Опционально

```bash
# Перейти в директорию admin-panel
cd billboard-mobile/admin-panel

# Установить зависимости
npm install

# Запустить dev сервер
npm run dev

# Откроется на http://localhost:3000
```

### Шаг 5: Создать тестовые данные

**Вариант 1: Через Web UI (Swagger)**

1. Откройте http://localhost:8000/docs
2. Раскройте `POST /api/v1/auth/register`
3. Нажмите "Try it out"
4. Введите данные:
```json
{
  "login": "car001",
  "password": "password123",
  "car_number": "01A001AA",
  "tariff": "standard",
  "driver_name": "Иван Иванов",
  "phone": "+998901234567"
}
```
5. Нажмите "Execute"

**Вариант 2: Через curl**

```bash
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "login": "car001",
    "password": "password123",
    "car_number": "01A001AA",
    "tariff": "standard",
    "driver_name": "Иван Иванов",
    "phone": "+998901234567"
  }'
```

### Шаг 6: Войти в Mobile приложение

1. Откройте приложение
2. Введите:
   - Логин: `car001`
   - Пароль: `password123`
3. Нажмите "Войти"

⚠️ **Примечание:** На этом этапе видео воспроизводиться не будут, так как в БД еще нет видео. Нужно загрузить видео через API.

---

## 📹 Загрузка видео (опционально)

### Подготовить тестовое видео

Вам нужен MP4 файл (рекомендуется H264 кодек, до 500 МБ).

### Загрузить через API

```bash
curl -X POST http://localhost:8000/api/v1/videos \
  -F "title=Тестовое видео 1" \
  -F "video_type=filler" \
  -F 'tariffs=["standard","comfort","business"]' \
  -F "priority=1" \
  -F "file=@/path/to/your/video.mp4"
```

Или через Swagger UI:
1. http://localhost:8000/docs
2. `POST /api/v1/videos`
3. Заполните форму и загрузите файл

### Загрузить контрактное видео

```bash
curl -X POST http://localhost:8000/api/v1/videos \
  -F "title=Контрактная реклама" \
  -F "video_type=contract" \
  -F "plays_per_hour=3" \
  -F 'tariffs=["standard","comfort"]' \
  -F "priority=10" \
  -F "file=@/path/to/contract_video.mp4"
```

### Проверить загруженные видео

```bash
curl http://localhost:8000/api/v1/videos | jq
```

---

## 🧪 Полный цикл тестирования

### 1. Создать автомобиль (если еще не создан)
### 2. Загрузить несколько видео (минимум 2-3)
### 3. Авторизоваться в Mobile приложении
### 4. Плейлист автоматически сгенерируется
### 5. Видео начнут воспроизводиться
### 6. Проверить аналитику

Через API:
```bash
# Получить токен
TOKEN=$(curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"login":"car001","password":"password123"}' | jq -r '.access_token')

# Получить аналитику
curl -X GET http://localhost:8000/api/v1/analytics/me \
  -H "Authorization: Bearer $TOKEN" | jq
```

---

## 🐛 Troubleshooting

### Backend не запускается

**Проблема:** `sqlalchemy.exc.OperationalError: could not connect to server`

**Решение:**
```bash
# Проверить что PostgreSQL запущен
docker ps | grep postgres

# Или запустить
docker-compose up -d postgres

# Или установить локально и запустить
brew install postgresql  # macOS
# apt install postgresql  # Linux

# Проверить подключение
psql -U billboard_user -d billboard_db -h localhost
```

**Проблема:** `redis.exceptions.ConnectionError`

**Решение:**
```bash
# Запустить Redis
docker-compose up -d redis

# Или локально
brew install redis  # macOS
redis-server
```

### Mobile не подключается к backend

**Проблема:** Network error, Connection refused

**Решение:**
1. Проверьте `baseUrl` в `lib/services/api_service.dart`
2. Для Android эмулятора используйте `10.0.2.2` вместо `localhost`
3. Для реального устройства используйте IP адрес компьютера
4. Убедитесь что firewall не блокирует порт 8000

```bash
# Узнать IP адрес (macOS)
ifconfig | grep "inet "

# Узнать IP адрес (Linux)
ip addr show

# Узнать IP адрес (Windows)
ipconfig
```

### Видео не воспроизводятся

**Проблема:** Черный экран или ошибка воспроизведения

**Причины:**
1. Нет видео в БД - загрузите через API
2. Неправильный формат видео - используйте MP4/H264
3. Проблемы с сетью - проверьте подключение

**Решение:**
```bash
# Проверить наличие видео
curl http://localhost:8000/api/v1/videos

# Проверить доступность файлов
ls backend/uploads/videos/

# Проверить плейлист (через mobile app или API)
curl -X GET http://localhost:8000/api/v1/playlists/current \
  -H "Authorization: Bearer $TOKEN"
```

---

## 📊 Проверка работы аналитики

### 1. Воспроизвести несколько видео в приложении

### 2. Получить статистику:

```bash
# Авторизация
TOKEN=$(curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"login":"car001","password":"password123"}' | jq -r '.access_token')

# Получить аналитику
curl -X GET http://localhost:8000/api/v1/analytics/me \
  -H "Authorization: Bearer $TOKEN" | jq

# Пример вывода:
# {
#   "vehicle_id": 1,
#   "car_number": "01A001AA",
#   "daily_stats": [...],
#   "video_stats": [...],
#   "total_earnings": 150000.0
# }
```

---

## 🚀 Готово к продакшену

### Backend

```bash
# Изменить SECRET_KEY в .env на случайную строку
# Настроить CORS для production домена
# Использовать gunicorn вместо uvicorn
pip install gunicorn
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker

# Настроить nginx как reverse proxy
# Настроить HTTPS (Let's Encrypt)
# Настроить автоматические бэкапы БД
```

### Mobile

```bash
# Изменить baseUrl на production URL
# Создать release build
flutter build apk --release  # Android
flutter build ios --release  # iOS

# APK будет в: build/app/outputs/flutter-apk/app-release.apk
```

---

## 📚 Дополнительная документация

- [README.md](README.md) - Общее описание
- [SETUP.md](SETUP.md) - Детальная установка
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура
- [API_EXAMPLES.md](API_EXAMPLES.md) - Примеры API
- [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) - Краткое резюме
- [STRUCTURE.txt](STRUCTURE.txt) - Структура проекта

---

## ✅ Чеклист запуска

- [ ] Python 3.11+ установлен
- [ ] Flutter 3.0+ установлен
- [ ] Backend зависимости установлены (`pip install -r requirements.txt`)
- [ ] PostgreSQL запущен (порт 5432)
- [ ] Redis запущен (порт 6379)
- [ ] Backend API запущен (порт 8000)
- [ ] Swagger доступен (http://localhost:8000/docs)
- [ ] Тестовый скрипт прошел успешно (`python test_api.py`)
- [ ] Создан тестовый пользователь (автомобиль)
- [ ] Загружено минимум 2-3 видео
- [ ] Mobile зависимости установлены (`flutter pub get`)
- [ ] baseUrl настроен в api_service.dart
- [ ] Mobile приложение запускается
- [ ] Успешная авторизация в приложении
- [ ] Плейлист генерируется автоматически
- [ ] Видео воспроизводятся
- [ ] Аналитика работает

---

## 🎉 Поздравляем!

Если все пункты чеклиста выполнены - ваша система **Billboard Mobile** полностью работает!

**Следующие шаги:**
1. Добавить больше видео
2. Создать больше автомобилей
3. Протестировать разные тарифы
4. Изучить аналитику
5. Настроить для production

**Нужна помощь?**
- Проверьте документацию выше
- Изучите примеры в API_EXAMPLES.md
- Просмотрите код в исходниках
