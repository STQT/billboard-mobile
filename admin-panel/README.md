# Billboard Admin Panel

Web-приложение для администрирования системы Billboard Mobile.

## Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Material-UI** - компоненты UI
- **React Router** - навигация
- **Axios** - HTTP клиент
- **Recharts** - графики (планируется)
- **Vite** - сборщик

## Функции

### ✅ Реализовано

- **Dashboard** - общая статистика
- **Управление автомобилями** - CRUD операции
- **Управление видео** - загрузка и управление
- **Аналитика** - фильтры для поиска
- **Плейлисты** - генерация и управление

### 🔄 В разработке

- Детальная аналитика с графиками
- Просмотр логов воспроизведения
- Финансовые отчеты
- Экспорт данных

## Установка

```bash
cd admin-panel

# Установить зависимости
npm install

# Запустить dev сервер
npm run dev

# Откроется на http://localhost:3000
```

## Сборка для production

```bash
npm run build

# Результат в папке dist/
```

## Структура проекта

```
admin-panel/
├── src/
│   ├── components/
│   │   └── Layout.tsx           # Главный layout с навигацией
│   ├── pages/
│   │   ├── Dashboard.tsx        # Главная страница
│   │   ├── Vehicles.tsx         # Управление автомобилями
│   │   ├── Videos.tsx           # Управление видео
│   │   ├── Analytics.tsx        # Аналитика
│   │   └── Playlists.tsx        # Плейлисты
│   ├── services/
│   │   └── api.ts               # API клиент
│   ├── types/
│   │   └── index.ts             # TypeScript типы
│   ├── App.tsx                  # Главный компонент
│   └── main.tsx                 # Точка входа
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

## API Integration

Админ панель подключается к Backend API через прокси (см. `vite.config.ts`):

```typescript
proxy: {
  '/api': {
    target: 'http://localhost:8000',
    changeOrigin: true,
  },
}
```

## Скриншоты

### Dashboard
- Карточки со статистикой
- Быстрые действия
- Графики (планируется)

### Автомобили
- Список всех автомобилей
- Добавление/редактирование
- Фильтрация по статусу

### Видео
- Загрузка видео
- Управление типами (филлер/контракт)
- Настройка тарифов

### Аналитика
- Фильтры по датам и автомобилям
- Детальная статистика
- Графики воспроизведений

## Использование

1. Запустите Backend API (должен работать на http://localhost:8000)
2. Запустите админ панель: `npm run dev`
3. Откройте http://localhost:3000
4. Навигация через боковое меню

## Troubleshooting

### CORS ошибки

Если возникают CORS ошибки, убедитесь что:
1. Backend запущен на http://localhost:8000
2. В Backend настроены CORS правильно (см. `backend/app/main.py`)
3. Используется прокси в Vite

### API не отвечает

1. Проверьте что Backend запущен: `http://localhost:8000/health`
2. Проверьте настройки прокси в `vite.config.ts`
3. Откройте DevTools и смотрите Network вкладку

## Development

### Добавление новой страницы

1. Создать компонент в `src/pages/`
2. Добавить роут в `src/App.tsx`
3. Добавить пункт меню в `src/components/Layout.tsx`

### Добавление нового API метода

1. Добавить метод в `src/services/api.ts`
2. Добавить типы в `src/types/index.ts`
3. Использовать в компонентах

## Production Deployment

1. Собрать проект: `npm run build`
2. Результат в `dist/`
3. Развернуть на web-сервере (nginx, apache, vercel, netlify)
4. Настроить переменные окружения для API URL

Пример nginx конфигурации:

```nginx
server {
    listen 80;
    server_name admin.billboard.com;
    
    root /var/www/billboard-admin/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## Лицензия

Часть проекта Billboard Mobile
